# LMS MTsN 2 CILACAP — PRODUCTION ENVIRONMENT TEMPLATE

# Server Configuration
PORT=3000
NODE_ENV=production
LMS_SESSION_SECRET=ReplaceWithStrongProductionRandomSecret2026!

# Database Configuration (MySQL 8.0 / MariaDB)
DATABASE_HOST=localhost
DATABASE_PORT=3306
DATABASE_USER=lms_user
DATABASE_PASSWORD=ReplaceWithStrongDatabasePassword2026!
DATABASE_NAME=db_lms

# Security & CORS
APP_URL=https://lms.mtsn2cilacap.sch.id
ALLOW_ORIGINS=https://lms.mtsn2cilacap.sch.id

# Upload Storage Limits (in MB)
MAX_UPLOAD_SIZE_MB=50
